name: Static dataset notice

on:
  workflow_dispatch:

jobs:
  static-data-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Dataset is checked into public/data; this repository does not query PostHog.'
